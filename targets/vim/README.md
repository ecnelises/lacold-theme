# Lacold for Vim and NeoVim

Copy a file from `colors/` to `~/.vim/colors/` or `~/.config/nvim/colors/`, set
`background` to `light` or `dark`, then run `:colorscheme lacold-air-blue`.

Complete color families follow `background` automatically.
