# Lacold for Codex CLI

Copy the desired `.tmTheme` to `$CODEX_HOME/themes/` (normally
`~/.codex/themes/`), then merge the matching TOML snippet into
`~/.codex/config.toml`. Pair it with a Lacold terminal profile.
