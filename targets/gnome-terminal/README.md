# Lacold for GNOME Terminal

Create a profile, find its UUID, then load the selected theme into that profile:

```sh
dconf load /org/gnome/terminal/legacy/profiles:/:PROFILE_UUID/ < lacold-air-blue-dark.dconf
```
