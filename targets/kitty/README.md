# Lacold for Kitty

Copy a `.conf` file to `~/.config/kitty/themes/`, then select it with
`kitten themes` or include it from `kitty.conf`.

For automatic appearance, copy the three files from one
`auto/lacold-air-COLOR/` directory into `~/.config/kitty/` and restart Kitty.
