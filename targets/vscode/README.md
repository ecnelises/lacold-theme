# Lacold for Visual Studio Code

Install the packaged VSIX with **Extensions: Install from VSIX...** or:

```sh
code --install-extension <downloaded-file>.vsix
```

Then open **Preferences: Color Theme** and select a Lacold theme.
